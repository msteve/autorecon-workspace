VITE_API_URL=http://localhost:8000
VITE_APP_NAME=AutoRecon V2









VITE_USE_MOCK_DATA=true# This is useful when the backend is unavailable or during frontend-only development# Set to 'true' or leave unset to use static mock data (default behavior)# Set to 'false' to attempt real API calls to the backend# Mock Data ConfigurationVITE_ENABLE_DEVTOOLS=trueVITE_APP_VERSION=2.0.0VITE_APP_VERSION=2.0.0
VITE_ENABLE_DEVTOOLS=true

# Mock Data Configuration
# Set to 'false' to attempt real API calls to the backend
# Set to 'true' or leave unset to use static mock data (default behavior)
# This is useful when the backend is unavailable or during frontend-only development
VITE_USE_MOCK_DATA=true
